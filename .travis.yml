sudo: required
language: node_js
services:
  - docker
branches:
  only:
  - master
before_install:
 - docker build -t minimalcss:tests .
script:
  #- yarn tsc
  #- yarn lintcheck
  - docker container run -it minimalcss:tests
  #- docker container run -it minimalcss:tests tsc
  #- docker container run -it minimalcss:tests lintcheck
