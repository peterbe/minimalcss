---
language: node_js
node_js:
  - 8
  - 7
  - 6
dist: precise
cache:
  yarn: true
  directories:
  - node_modules
branches:
  only:
  - master
install:
  - yarn install
script:
  - yarn tsc
  - yarn lintcheck
  #- if [ "${NODE_ENV}" = "development" ]; then yarn codecov; fi
